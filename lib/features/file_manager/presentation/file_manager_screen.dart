import '../../../core/widgets/section_placeholder_page.dart';

class FileManagerScreen extends SectionPlaceholderPage {
  const FileManagerScreen({super.key})
    : super(
        title: 'Archivos',
        description:
            'Aqui se implementara el gestor de archivos con operaciones permitidas por plataforma.',
      );
}
